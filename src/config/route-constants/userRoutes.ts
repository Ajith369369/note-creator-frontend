// Legacy user routes - kept for backward compatibility
// All properties are unused - migrated to SHARED and ADMIN
// This export is kept for backward compatibility with frontendRoutes.ts
export const USER = {} as const;
